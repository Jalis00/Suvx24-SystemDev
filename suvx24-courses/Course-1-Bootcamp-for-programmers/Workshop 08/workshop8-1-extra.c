#include <stdio.h>

int main() {
    
    // Initialization = Declaration + Assignment
    int n = 0;  

    while(n <= 10)
    {
        printf("\nHela mitt namn");
        //printf("\nLoop varaibel n = %i", n);
        n++;
    }

    return 0;
}