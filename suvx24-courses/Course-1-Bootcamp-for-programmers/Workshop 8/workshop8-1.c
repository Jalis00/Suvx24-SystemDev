#include <stdio.h>

int main() {
    
    // Compare this code with the solution using while-statement
    // Which one fits the prbolem best?
    for(int i = 0; i <= 10; i++)
    {
        printf("\nMy first name and last name.");
        //printf("\nLoop varaibel i = %i", i);
    }

    return 0;
}